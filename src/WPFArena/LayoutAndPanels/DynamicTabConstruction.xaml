<Window x:Class="WPFArena.LayoutAndPanels.DynamicTabConstruction"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WPFArena.LayoutAndPanels"
        mc:Ignorable="d"
        Title="DynamicTabConstruction" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="tabHeaderTemplate">
            <StackPanel Orientation="Horizontal" Margin="4">
                <Ellipse Stroke="Black" StrokeThickness="1" Fill="Blue" Width="16" Height="16" />
                <TextBlock Margin="4,0,0,0" VerticalAlignment="Center" FontSize="15" Text="{Binding Header}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="tabContentTemplate">
            <Grid>
                <TextBlock FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Text}" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <TabControl x:Name="_tabs">
        <TabControl.ItemContainerStyle>
            <Style TargetType="TabItem">
                <Setter Property="HeaderTemplate" Value="{StaticResource tabHeaderTemplate}" />
                <Setter Property="ContentTemplate" Value="{StaticResource tabContentTemplate}"></Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Yellow"></Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </TabControl.ItemContainerStyle>
    </TabControl>
</Window>
