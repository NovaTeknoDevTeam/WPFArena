<Window x:Class="DataTemplates.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataTemplates"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:Person}">
            <Border BorderBrush="Green" BorderThickness="2">
                <StackPanel Margin="4">
                    <TextBlock Foreground="Red" FontSize="20" Text="{Binding Name}" TextAlignment="Center" />
                    <TextBlock Foreground="Red" FontSize="16" Text="{Binding Age}" TextAlignment="Right" />
                </StackPanel>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:Employee}">
            <Border BorderBrush="Black" BorderThickness="1">
                <StackPanel Margin="4" Orientation="Horizontal">
                    <TextBlock Foreground="Blue" FontSize="20" Text="{Binding Name}" TextAlignment="Center" />
                    <TextBlock Foreground="Red" FontSize="16" Text="{Binding Department}" Margin="20,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto">
            </RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Content="{Binding}" Grid.Row="0">
            <Button.ContentTemplate>
                <DataTemplate>
                    <Border BorderBrush="Green" BorderThickness="2">
                        <StackPanel Margin="4">
                            <TextBlock Foreground="Red" FontSize="20" Text="{Binding Name}" TextAlignment="Center"></TextBlock>
                            <TextBlock FontSize="16" Text="{Binding Age}" TextAlignment="Right"></TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>
        <!-- <ListBox Grid.Row="1" x:Name="_list" HorizontalContentAlignment="Stretch" ItemsSource="{Binding}" /> -->
        <ListBox HorizontalContentAlignment="Stretch" ItemsSource="{Binding}" Grid.Row="1"/>
    </Grid>
    
</Window>
