<Window x:Class="ValueConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ValueConverter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:GeneralForecastToBrushConverter x:Key="gf2brush"></local:GeneralForecastToBrushConverter>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Select number of days to forecast:" FontSize="15" VerticalAlignment="Center" Margin="4"></TextBlock>
            <ComboBox x:Name="_days" SelectedIndex="0" Width="50" />
            <Button Margin="4" Content="Get Forecast" FontSize="16" Click="GetForecast_OnClick"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="4" TextBlock.FontSize="15">
            <TextBlock Text="Select units:" Margin="4"></TextBlock>
            <RadioButton Content="Celsius" IsChecked="True" Margin="10,4" />
            <RadioButton Content="Fahrenheit" Margin="20,4" />
        </StackPanel>
        <ListBox ItemsSource="{Binding}" Grid.Row="2" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="4" BorderBrush="Black" Padding="4" BorderThickness="2" Background="{Binding generalForecast, Converter={StaticResource gf2brush}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding generalForecast}" />
                            <TextBlock FontSize="16" Margin="10,0,0,0" VerticalAlignment="Bottom" Text="{Binding temperatureLow, StringFormat = 'Low: \{0:N2\}'}" />
                            <TextBlock FontSize="16" Margin="10,0,0,0" VerticalAlignment="Bottom" Text="{Binding temperatureHigh, StringFormat = 'High: \{0:N2\}'}" />
                            <TextBlock FontSize="16" Margin="20,0,0,0" VerticalAlignment="Bottom" Text="{Binding precipitation, StringFormat = 'Percip: \{0:N2\}'}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
